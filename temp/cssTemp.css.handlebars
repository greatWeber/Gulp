 
@function getUrl($name) {
	@if $name == 'default.png'{
		@return '../sprites/'+$name;
	}

	@if $name != 'default.png'{
		@return '../../sprites/'+$name;
	}
}
 {{#sprites}}
.{{class_name}}-{{name}} {
      display: inline-block;
      background-image: url(getUrl('{{{css_image}}}'));
      background-position: {{px.offset_x}} {{px.offset_y}};
      width: {{px.width}};
      height: {{px.height}};
    }
{{/sprites}} 